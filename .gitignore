# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
*~

# Editor and IDE files — track shared VS Code settings, ignore the rest
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
*.swo
*~
.*.sw[op]

# Backup files
*.bak
*.backup
*.old
*.orig

# Log files
*.log

# Temporary files
tmp/
temp/
*.tmp

# Cache directories
__pycache__/
*.pyc
.cache/
cache/

# Fish shell history and private data
.config/fish/fish_history
.config/fish/fishd.*

# System specific
.directory
.Xauthority

# Private/sensitive files
*_private
*_secret
*.key
*.pem
.env
.env.local

# Git specific
.git/

# Build outputs
build/
dist/
*.o
*.so
*.a

# Node modules — the repo's own tooling is node-free (dprint + rumdl via mise),
# but keep this so a stray `npm install` in a sub-project is never committed.
node_modules/

# mise local install cache / rumdl cache
.rumdl_cache/

# chezmoi machine-local config — holds your prompted private values. NEVER commit.
.config/chezmoi/

# AI assistant / tool state — not part of the curated dotfiles.
# Un-ignore a specific path if you decide to track it.
.claude/
.agents/
.gemini/
# rtk: track filters.toml (shareable), but not config.toml (machine/telemetry state)
home/dot_config/rtk/config.toml
**/.claude/settings.local.json
skills-lock.json

.docs
